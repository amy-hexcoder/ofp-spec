{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openfp.dev/schemas/0.3/well-known.schema.json",
  "title": "OPENFP well-known document",
  "description": "Served at https://<domain>/.well-known/openfp.json. Either a full manifest, or a pointer to one.",
  "type": "object",
  "required": ["openfp"],
  "properties": {
    "openfp": { "type": "string", "pattern": "^\\d+\\.\\d+$" },
    "site_id": { "type": "string" },
    "manifest": { "type": "string", "format": "uri" },
    "verification": { "type": "string", "description": "Domain-ownership token for the registry. Agents ignore it." }
  }
}
